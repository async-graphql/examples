directive @defer(
  """Relay fragment label"""
  label: String
) on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT

directive @stream(
  """Relay fragment label"""
  label: String
) on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT

type Book {
  title: String!
  author: String!
  comments: [Comment!]
}

type Comment {
  user: String!
  text: String!
}

type Query {
  books: [Book!]!
}

